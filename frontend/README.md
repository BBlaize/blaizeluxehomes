# Blaize Luxe Homes — Frontend

Vite + React + TypeScript landing page for Blaize Luxe Homes.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run preview
```

Open the local URL printed by `npm run dev` (usually http://localhost:5173).
